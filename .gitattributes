*.sqlite.zst filter=lfs diff=lfs merge=lfs -text
*.tar.zst filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
data/private/cardz-active-bootstrap.tar.gz filter=lfs diff=lfs merge=lfs -text
data/private/images/**/* filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.webp filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.json text eol=lf
*.mjs text eol=lf
*.ts text eol=lf
*.tsx text eol=lf
*.md text eol=lf
# SVG 一定要釘 LF：`scripts/test-fe-brand-logo-svg.mjs` ⑦ 對 `<svg` 之後 body 嘅 sha256
# stamp，而個 stamp 係喺 LF 之下計嘅。冇呢句就跌返 core.autocrlf ——
# Windows checkout 出 CRLF、sha 即刻對唔返，`npm test` 喺一部乾淨機上面無端端紅，
# 而 diff 睇落一個字都冇改過（2026-08-18 加 SVG wordmark 嗰陣 git 已經出咗
# 「LF will be replaced by CRLF」警告，先發現）。
*.svg text eol=lf

# 部署到 Linux 嘅檔一律 LF，唔理 core.autocrlf。CRLF 嘅 .sh 喺 Linux 係
# 「bad interpreter: /usr/bin/env bash^M」即死；systemd unit 嘅 ExecStart 值
# 尾巴帶 \r 會變成揾唔到嘅路徑。呢啲檔喺 Windows working tree 都要保持 LF，
# 否則由 WSL 經 /mnt/c 直接跑就會中招。
*.sh text eol=lf
*.bash text eol=lf
*.service text eol=lf
*.timer text eol=lf
*.py text eol=lf
*.sql text eol=lf

# 退役 migration 係 byte-frozen 事故證據：migration_policy.py 用 sha256(disk bytes)
# 驗佢哋，cardz_migration_ledger 亦記住同一個 hash。上面 *.sql 嘅 eol 正規化會令
# fresh checkout 攞到 LF 版、同 pin 對唔上，assert_retired_hashes 喺 migrate() 第一步
# 就會炸。呢三個檔案豁免 eol 政策，blob 儲 exact bytes，邊度 checkout 都同 pin 一致。
pipelines/migrations/046_legacy_quote_unique_owner.mysql.sql -text
pipelines/migrations/047_legacy_quote_nondestructive_recovery.mysql.sql -text
pipelines/migrations/047_restore_legacy_quote_append_only.mysql.sql -text
# 字體 binary：test-fe-font-contract.mjs 用 sha256(disk bytes) 對返 index.ts / OG route
# 檔頭抄嗰個 hash。呢度唔明寫 `-text` 就淨係靠 git 嘅 binary 自動偵測 —— 一個 checkout
# 揀錯就 eol 正規化，hash 對唔上，而個 test 會喺人哋 clone 完第一次跑就無端紅。
# 順帶：字體**唔行 LFS**（上面 `*.png` 果條唔覆蓋佢哋）。呢個 repo 嘅 deploy 係
# 有 smudge 嘅（`public/brand/*.png` 全部係 LFS pointer，而 OG route 用 sharp 讀原圖
# 出到正常 logo），所以唔係「一定唔得」；只係字體 130 bytes 一個 pointer 換嚟嘅
# 失敗模式（全站跌 fallback 字，冇 error）唔值得為咗慳嗰 380 kB 去冒。
*.ttf -text
*.otf -text
*.woff -text
*.woff2 -text
*.yml text eol=lf
*.yaml text eol=lf
*.toml text eol=lf
Dockerfile text eol=lf
*.env.example text eol=lf
