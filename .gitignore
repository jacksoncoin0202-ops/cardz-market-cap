.env
.env.*
!.env.example
node_modules/
**/node_modules/
.next/
**/.next/
dist/
**/dist/
coverage/
data/runtime/
data/private/
__pycache__/
*.pyc
*.log
*.tmp
*.bak
.DS_Store
Thumbs.db
.venv-backend-windows/

# 本機 fe03 開發工具（3800 live-db 預覽）。呢兩個檔一路都唔係 gitignore，
# 所以每次 git add 都要人手記得避開 —— 已經漏過一次入咗 commit。靠記性 =
# 一定會再犯，所以喺呢度封死。
fe03-server.ps1
scripts/fe03-verify.cjs
# .claude/launch.json 都係本機 dev server 設定，同上面一齊漏入過 commit。
# 用 `.claude/*` 而唔係 `.claude/`：git 唔可以喺一個已排除嘅**目錄**入面 re-include 檔，
# 寫成 `.claude/` 就永遠 negate 唔返。
.claude/*
# 例外：`.claude/agents/*.md` 係 repo 嘅一部分（fe-design-review 係 FE05 嘅阻斷式設計 gate，
# apps/web/DESIGN.md 直接引住佢）。只喺本機存在 = 換一棵 clone／換一個 agent 就冇咗個 gate。
!.claude/agents/
.claude/agents/*
!.claude/agents/*.md
