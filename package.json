{
  "name": "cardz-market-cap",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @cardz/web run dev",
    "build": "npm run build --workspace @cardz/web",
    "test": "node scripts/run-python.mjs -X utf8 scripts/run_all_tests.py"
  }
}
