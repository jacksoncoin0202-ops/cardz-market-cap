*
!package.json
!package-lock.json
!.npmrc
!tsconfig.base.json
!apps
!apps/web
apps/web/node_modules
apps/web/.next
!packages
packages/*/node_modules
packages/*/dist
!data
data/*
!data/public
data/public/*
# seed / box sidecar 留喺 context 做無-compose 後備。runtime 由 compose volume 蓋過。
# sitemap.ts 而家 force-dynamic，唔再靠 build 當刻嗰份。
!data/public/seed-snapshot.json
!data/public/box-subset.json
# data/public/market-assets（1.4GB）刻意唔入 context：build 期冇任何一步讀佢
# （prepare-standalone.mjs 仲主動刪走 standalone 入面嘅 public/market-assets），
# runtime 先由 compose 嘅 :ro volume 掛入 /app/data/public/market-assets。
# 入返 context 嘅話每次 build 會多食約 4.3GB build cache（實測）。
**/node_modules
**/.git
**/*.log
