# Production web (app.cardzmarketcap.com)
# Server deploy path: git pull → docker compose up --build
#
# Build context MUST be repo root (npm workspaces + data/public).
# Data is baked at build time from data/public/seed-snapshot.json + market-assets.
# For demo/non-productionEligible snapshots, set MARKET_DATA_ALLOW_DEMO=true.

name: cardz-market-cap-web

services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        CARDZ_PUBLIC_BUILD_ID: ${CARDZ_PUBLIC_BUILD_ID:-deploy}
    image: cardz-web:latest
    ports:
      - "${CARDZ_WEB_PORT:-3000}:3000"
    environment:
      CARDZ_RUNTIME: node
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      # Required until generation.productionEligible === true
      MARKET_DATA_ALLOW_DEMO: ${MARKET_DATA_ALLOW_DEMO:-true}
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
