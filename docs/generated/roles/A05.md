# A05 — GemRate POP and Grader

> Durable generated role contract. It intentionally contains no live counts,
> current prices, current completion claims, or installed-plugin state.

## Exact load envelope

1. Read [`AGENTS.md`](../../../AGENTS.md).
2. Read [`DOCUMENT_AUTHORITY.md`](../DOCUMENT_AUTHORITY.md).
3. Read this `A05` pack only; do not read the other eleven role packs.
4. Run `python -X utf8 scripts/backend.py explain A05`.
5. Run `python -X utf8 scripts/backend.py explain <ASSIGNED_WORK_ITEM_ID>`.
6. Before a required document, run `python -X utf8 scripts/backend.py explain document:<DOCUMENT_ID>`.
7. Load one required document or one triggered capability at a time.
8. Load volatile baseline, PROJECT_STATE slice, cohort and input receipts only
   when named by the work item or dispatcher prompt.

## Stable role contract

- Layer: `L2`.
- Mode: `HARVEST_CANDIDATE_IMPLEMENT`.
- Stage(s): `A05`.
- Mission: Close current POP, POP history, grader supply, alias and freshness cells without synthesizing history.
- Architecture nodes: `authority.gemrate`, `transport.gemrate-direct`, `transport.gemrate-public`, `transport.gemrate-mirror`.

### Read scope

- `pipelines/gemrate_source.py`
- `pipelines/gemrate_candidate_backfill.py`
- `data/runtime/private-source-runs`

### Required documents — demand-load only

- [`gemrate-source`](../../../pipelines/GEMRATE_SOURCE.md) — first run `backend.py explain document:gemrate-source`; load only when the current decision needs it.

### Owned write claims

- `gemrate-domain` (`exclusive`)
  - `pipelines/gemrate_source.py`
  - `pipelines/gemrate_candidate_backfill.py`
  - `pipelines/gemrate_brute_harvest.py`
  - `tests/test_gemrate_transport.py`
  - `tests/test_gemrate_candidate_backfill.py`

No file outside these claims becomes writable through delegation, capability
loading, repository discovery, or another Agent's request.

### Capability routes — trigger before load

- `code-navigation`
  - Load when: an exact role node, field, caller, writer or work item needs source impact evidence.
  - Candidate adapters: `rg`, `CodeGraph`.
  - If absent: use scoped rg when available or report a capability gap; never invent a call edge.
  - Output boundary: `evidence_not_authority`.

Adapter availability is volatile material discovered at runtime. A listed
candidate is not proof that a plugin or skill is installed, connected, loaded,
or authorized. Read exactly one selected `SKILL.md` only after its trigger is true.

## Child-Agent rules

- Default: `read_only_same_role`.
- children inherit the parent baseline, role, blocked actions and no additional authority.
- return a structured dependency request to MAIN; do not silently spawn a cross-role writer.
- A child writer requires MAIN to partition a non-overlapping subclaim and must
  return to this parent for integration; otherwise the child is read-only.
- Every child prompt must name:
  - `parent_agent_id`
  - `agent_id`
  - `wave_id`
  - `role_stage`
  - `role_mode`
  - `work_item_id`
  - `baseline_id`
  - `baseline_manifest`
  - `baseline_manifest_sha256`
  - `cohort_sha256`
  - `routing_sha256`
  - `db_state_fingerprint`
  - `mode`
  - `write_claim_id`
  - `allowed_tool_ids`
  - `read_set`
  - `write_set`
  - `generated_set`
  - `forbidden`
  - `input_artifacts`
  - `dependency_receipts`
  - `output_root`
  - `acceptance`
  - `handoff_to`

## Blocked actions

- `direct_canonical_sql`
- `snapshot_publish`
- `pointer_write`
- `timer_change`

## Feedback decision

- `FACT_UPDATE`: update only via the role-owned candidate/registered writer with
  provenance, freshness and read-back evidence; do not edit architecture.
- `IMPLEMENTATION_DEFECT`: fix only inside the existing claim and acceptance.
- `CROSS_ROLE_DEPENDENCY`: pause the dependent write and route a structured request to MAIN.
- `CONTRACT_GAP`: stop the affected decision and propose a versioned contract change.
- `INVARIANT_CONFLICT`: stop; only DADDY + MAIN may accept and version the change.
- `SECURITY_OR_PRIVACY`: stop the affected path and return redacted evidence.

The feedback record must contain:

- `feedback_id`
- `agent_id`
- `work_item_id`
- `baseline_id`
- `category`
- `blocked_rule_or_contract`
- `evidence`
- `impact`
- `proposed_owner`
- `proposed_change`
- `can_continue_read_only`

## Acceptance

- all cohort POP cells reach a terminal state.
- no inferred identity or synthesized history.

## Handoff

- Next role(s): `A10`, `A11`, `A12`.
- Return the common handoff fields below with exact commands and raw output:
  - `parent_agent_id`
  - `agent_id`
  - `wave_id`
  - `role_stage`
  - `role_mode`
  - `work_item_id`
  - `baseline_id`
  - `baseline_manifest_sha256`
  - `cohort_sha256`
  - `routing_sha256`
  - `db_state_fingerprint`
  - `write_claim_id`
  - `scope`
  - `read_set`
  - `write_set`
  - `generated_set`
  - `input_artifacts`
  - `output_artifacts_with_sha256`
  - `dependency_receipts`
  - `commands_run`
  - `test_output`
  - `raw_test_output`
  - `acceptance_verdict`
  - `zero_mutation_proof`
  - `risks`
  - `blockers`
  - `feedback_records`
  - `next_owner`
  - `intent_fidelity`
  - `yagni`

Do not turn a changing fact into a hard-coded rule, a missing value into zero,
a discovered adapter into authority, or a blocked contract into a silent fallback.
