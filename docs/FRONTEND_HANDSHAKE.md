# FRONTEND_HANDSHAKE — superseded

The previous `indexes.<code>.members[]` / `cards.<id>` description does not
match the current typed snapshot and cannot direct implementation.

Current authority:

- [`DATA_CONTRACT.md`](DATA_CONTRACT.md) for semantics
- `packages/market-data/src/schema.ts` for public property shape
- `packages/market-data/src/validate.ts` for closed-schema validation
- [`generated/DATA_LINEAGE.html`](generated/DATA_LINEAGE.html) for generated lineage

The old document is preserved under
`archive/2026-07-29-document-reset/FRONTEND_HANDSHAKE_LEGACY_20260729.md`.
