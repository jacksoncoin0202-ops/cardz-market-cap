# Beta 上線計劃 — 2026-07-26 開階段

定位：新階段開工文檔。上一階段盤點喺 [STAGE_REVIEW_20260726.md](STAGE_REVIEW_20260726.md)（凍結快照，唔准改）。
現況數字以 [../PROJECT_STATE.md](../PROJECT_STATE.md) 為準；本文只鎖定**目標、次序、驗收**。

## 0. 操作人決策（2026-07-26，唔准自行推翻）

1. **目標係「足夠上線 Beta」**——數據會喐、係真數就得。唔追求完備。
2. **次序：① 前端展示數據補齊 → ② DB 整理 → ③ 自動化。③ 押後到最尾先研究。**
3. **G10 做底**：欄位以 G10 建立嘅 schema 為基準，其他源填返上去。
4. **補數用腳本，唔用 API**（「API 用唔到」——GemRate key ~07-29 到期，之後只有 keyless 腳本路徑）。
5. **eBay 成交價做 refill**——補 DB 唔夠嘅位，唔係日更源（見 §2.4）。
6. Beta 冇問題就推上線。推 production 前要操作人親自拍板（沿用舊規）。

## 1. 開工實況（07-26 實測，唔係抄文檔）

- **日更鏈係活嘅**：`verify_daily_run.py` 07-26 10:38 UTC 實跑——3 個指數都有
  effective_date≥07-25 新行，今日 UTC 已寫 60,249 條 source obs。
  唯一 FAIL 係 `volume_floor`：ebay 66→0 / snkrdunk 142→0（ghost source 清零，預期），
  gemrate 80%、snk_psa10 56%（要睇多兩日）。
- **Git**：remote 得 `origin/main`（舊）。工作 branch `ui-experiments-20260725` + 大量
  未 commit 檔案全部未推。呢個階段當新工作處理，穩定後先整理 commit/push。
- **前端 1d/7d/30d 切換 UI 已存在**（rankings.tsx `period: "1d"|"7d"|"30d"`），
  缺嘅係下面 §2 嘅數據覆蓋，唔係 UI 位。
- 上線快照（id=19，07-25，255 卡）覆蓋：價格/POP/市值 255/255、圖 251、四語故事 236、
  價格 delta 1d 255 / 7d 237 / 30d 232、**POP delta 7d/30d 0/255**、sparkline 251。

## 2. Part ①：前端展示數據補齊（全部用 DB/本機現有數據，唔使新採集）

| # | 缺口（07-25 快照實數） | 根源／修位 | 驗收（喺快照層數，唔係 UI 層估） |
|---|---|---|---|
| 1.1 | POP delta 7d/30d **0/255** | 週級 POP 史已喺本機（`data/private/gemrate/cards/<sha1>/history_full.json`，701 檔×157 週點），`population_series()` 識讀；斷喺 `canonical_public_snapshot.py` 組裝層冇接線 | 有 history 嘅上線卡 POP delta 全填；冇 history 嘅 fail-closed 出 null，唔准借價格 delta 頂 |
| 1.2 | 價格 delta 7d 237 / 30d 232，未夠 255 | 大概率係嗰啲卡 7/30 日前冇觀察行。G10 有 638 條 K 線＋641 檔成交史可 backfill 基線 | 先列出缺嘅 18/23 張卡＋逐張缺因，再決定 backfill；目標 ≥95% |
| 1.3 | `topGrade` 出字面 `"top"`（P2-3 bug） | snapshot producer 字段錯 | 快照無 literal "top"；前端 grade label 正常 |
| 1.4 | TAG 220 卡 07-29T00:00Z 新鮮度過期（炸彈） | `canonical_public_snapshot.py` `latest_populations()` 加 >168h date floor → 標 `unavailable`/`value=null` | 07-29 後 TAG 卡顯示 unavailable，唔係負 delta、唔係舊數扮新 |
| 1.5 | 故事四語 236/255；watchlist 151 條缺故事 | 數據源 `catalog_variant_locale`（1,200 行）＋ G10 研究文 480 篇；缺嘅係翻譯／接線 | 上線 255 卡故事四語 100%；watchlist 故事策略定案（補齊或明確唔顯示） |
| 1.6 | 卡圖 251/255（缺 4 張） | G10 卡圖 594 張已入庫，查嗰 4 張斷喺邊個環節 | 255/255 |

**Part ① 唔做**：新增 UI 功能、改版面。呢部分係「填滿現有位」。

## 3. Part ②：DB 整理（G10 做底，收斂成一份乾淨數據）

| # | 事項 | 現況（07-26 量） | 做法 |
|---|---|---|---|
| 2.1 | 卡目重複收斂 | `catalog_variant` 24 組重複（52 行，3.3%）；fact 表 0% 重複；收斂目的地 `catalog_printing_identity` 得 **0 行** | 唔係 delete 行——建收斂層：每組指定 canonical variant，其餘掛 alias。join 一律行 `catalog_source_identity`，唔准用卡名 |
| 2.2 | 身份裁決積壓 | `market_identity_review_queue` **203 條 pending**（全 G10 family）＝ DATA_GAPS 批③ | 人手裁決 session 一次過清（最高 ROI，順手解上線 blocker）。裁決係 taste 類，要操作人／PM 做，我準備好裁決清單 |
| 2.3 | 隔離區分流 | quarantine 70,474 ＋ rejected 32,738 未 review | 唔逐條睇——按 reason code 分桶＋每桶抽樣驗證，再批量處理。目標係「知道每桶係咩、點解喺度」，唔係清零 |
| 2.4 | 腳本補數（唔用 API） | GemRate key ~07-29 死 | keyless 路徑：`gemrate_source.py collect` / `public-card-dump` / `pop-report` ＋ 本地 G10 mirror fallback（`DEFAULT_G10_MIRROR_ROOT`）。SNK：`snk_psa10` feed 生存中（07-26 有 117 行）。**eBay refill**：G10 本機 9,489 條 sold（559 卡）vs DB 3,824 行（97.6% 係 07-25 一次 backfill）——用 G10 eBay 成交史補 `market_sale_observation`，唔當日更源 |
| 2.5 | backfill 衛生 | 07-27 `g10_analytics 1277` 必出一日假紅（STAGE_REVIEW §3.4） | 所有 refill 寫入帶 `ingest_mode='backfill'`；`_volume_floor_check()` 基線剔除 backfill 行。呢條要落實先好開 2.4 嘅大 refill |
| 2.6 | normalizer 統一 | `normalize_collector` 三個實現散喺 11 檔 | 收斂做一份 shared module——2.1/2.2 嘅裁決結果要靠同一把尺先企得穩 |

**次序建議**：2.5 → 2.1 → 2.2（要人）→ 2.4 refill → 2.3 → 2.6 隨手。

## 3.5 進度記錄（做完先寫，寫實測數）

### 2026-07-26 晚 — 1.1 診斷閉環 + 1.3 / 1.4 落地（agent: claude-beta-plan）

- **1.4 TAG date floor ✅ 落地**：`canonical_public_snapshot.py` 加
  `POPULATION_STALE_HOURS = 168` + `population_is_stale()`；`card_from_row()`
  grader loop 頭過期即 `observed = None`，label／total／POP／changePct 四樣
  全部自動歸 unavailable。基準係快照 `effective_at`（唔係跑機當刻），同一
  generation 幾時重跑都出一樣結果。PSA 主鏈（`populationPsa10`）冇郁——PSA
  斷 7 日屬於成個快照都應該炸嘅級數，維持 hard raise。
- **1.3 topGrade ✅ 落地**：`top_grade_label()` 將字面 `'top'`／空值回退
  `TOP_GRADE[grader]`，真 label 直出。regen 實測：literal "top" **1,108 → 0**，
  1,215 個 grader block 全部出 "10"。ingest 層根修（9,862 行 DB label）擺 Part②。
- **1.1 POP delta ✅ 診斷閉環，唔使改 code**：接線本來就通。07-26 regen 實測
  PSA 7d ready 67 / unavailable 52 / accumulating 124；30d ready 119。
  52 張 7d unavailable 唔係 bug——樣本卡（085/SVP，variant 1）證實 GemRate
  週線實際係**雙週點**（06-23→07-07→07-21），7d 窗口 ±2 日內根本冇觀測，
  fail-closed 正確；30d 窗口命中 06-23 錨（+0.90% ✓）。DB 日線 07-21 起步，
  日更鏈跑落去 08-01 前後呢批自然轉 ready。accumulating 124 張 = 冇本機週線
  史，等 Part② 2.4 refill。
- 測試：`test_canonical_public_snapshot.py` 新增 3 個 test（date floor 過期
  fail-closed／168h 邊界／label 回退），48 passed。
- 注意：快照出街 card id ≠ DB `opaque_id`（tier-1/2 卡 id 由 presentation pack
  凍住抬過嚟）。由出街 id 反查 DB 要行 collector_number 或 producer 嘅對照表。
  （DB `collector_number` 存 subset 頭段 `GG69`；快照 display 係 `GG69/GG70`，
  反查要 split 頭段。）

### 2026-07-26 晚 — 1.2 價格 delta 缺卡診斷閉環（唔使改 code，修法收斂到 2.4）

07-26 fresh 快照（243 published）：7d ready **225** / accumulating 18；30d ready
**226** / accumulating 17（28 張 distinct 卡）。逐張查 `market_price_observation`
日期分佈，**28/28 全部係「觀測歷史唔夠深／唔夠密」，冇任何接線 bug**：

- 上游計法（[market_alerts.py](../pipelines/market_alerts.py) `pick_observation`）
  係**單向窗**：prior 只准 target 前 ≤3 日嘅行，target 之後嘅行唔計（設計正確，
  攞後面嘅行會令「7 日變動」縮水）。我一開始用 ±tolerance 雙向窗診斷出「有錨但
  NULL」係腳本同上游語意唔一致，唔係上游錯。
- 三類缺因：**新卡**（GG69/GG44/SV107/TG20/SM191/SWSH261 等，07-13〜07-19 先開始
  有觀測）；**舊卡觀測疏**（006/034、SM168、SM210、ST10-006、SWSH075、SWSH144，
  6 月尾窗口冇行）；**啱好差一日**（230/XY-P 型：07-19 有行但 7d target 07-18
  嘅前向窗係 07-15..07-18）。
- 修法唔係改 code：(a) 日更鏈行落去 7d 窗 8 月頭自然飽和；(b) 30d 靠 **2.4 G10
  K 線 backfill**（638 條 K 線／641 檔成交史 → `market_price_observation`，
  ingest_mode='backfill'，2.5 衛生先行）。1.2 至此閂 file，剩返睇 2.4。

**Side findings（記帳，未修）**：
1. **1d=0% artifact**：`pick_observation` current 同 prior(1d) 可以揀中**同一行**
   （current 行係 07-24、effective 07-25 時，1d target 07-24 又係嗰行 → 0.000000）。
   07-26 快照 1d ready 243/243 入面 **114 張 value=0**，當中含呢種假 0。根修喺
   `market_alerts.py`（prior 唔准解析到 current 同一行，冇更舊行就出 null）——
   屬指數 pipeline，要另外 claim + 影響評估，唔喺今次順手改。
2. **SV49 / TG20 各有一個 0 觀測嘅重複 variant**（vid 1151 / 619）——2.1 收斂
   嘅現成案例。

### 2026-07-26 深夜 — contentSha256 跨語言 bug 根修 + production 快照上咗 :3800（agent: claude-beta-part1）

- **contentSha256 修好（producer 側）**：TS `publicSnapshotContentSha256` 係 parse 完
  restringify 先 hash，Python 就 hash 自己嘅序列化文字——整數值 float Python 寫
  `0.0`、JS 重寫做 `0`，兩份文字差 430 bytes，hash 永遠對唔上（實測 6 個 distinct
  整數值 lexeme，`0.0` 佔 210 次；16,015 個非整數 float 零 mismatch）。修法：
  `canonical_public_snapshot.py` 加 `js_safe_numbers()`，`stable_json` 同 `atomic_json`
  行同一份正規化（整數值 float → int；non-finite／指數寫法 fail-closed raise）。
  `validate.ts` 一行冇郁。測試 +3（`JsSafeNumbersTests` 釘死序列化 bytes），33 passed。
- **本機 :3800 接上 production 快照**：`apps/web/src/lib/server-snapshot.ts` dev 分支
  加 guard——設咗 `MARKET_DATA_SNAPSHOT_PATH` 先行 `loadNodeSnapshot()`（同生產同一條
  `assertPublicSnapshot({production:true})` 閘），冇設照舊派 demo seed，預設行為零改動。
  路徑喺 gitignored `apps/web/.env.local` 指住 `data/runtime/local-serve/snapshot.json`
  （`canonical_20260725_aaca5dd2ee9f`，top100 100／watchlist 143，TS validator
  production:true PASS）。實測 :3800 主頁 HTML demo seed 零出現、`2026-07-25` 1,132 處；
  `/card/<top1>`、`/graders/PSA`、`/watchlist`、`/api/v1/market` 全 200；tsc 乾淨。
  git 入面嘅 `data/public/seed-snapshot.json` 一個 bit 冇掂。
- 換數據要重啟 dev server（`loadNodeSnapshot` 進程內 cache）；delete `.env.local`
  即退返 demo seed。

## 4. Part ③：自動化 — 押後

唔研究、唔起草。而家唯一保留嘅自動化係已有嘅每日 09:30 JST run＋各閘。

## 5. 風險日曆（呢個星期）

| 日期 | 事件 | 對策 |
|---|---|---|
| 07-27 | `g10_analytics 1277` backfill → volume_floor 假紅 | 見 2.5；未修前當已知假警報 |
| 07-27/28 05:47 | GemRate 凍結掃 ×2（上次 40.7% stalled，已改 `--speed slow`） | 睇進度，唔追寫滿 |
| ~07-29 | GemRate key 到期 | 切 keyless 路徑（2.4），或操作人換新 key |
| 07-29T00:00Z | TAG 220 卡新鮮度過期 | 1.4 date floor 要喺呢日前落地 |
| 每日 | 失敗無聲：`CARDZ_ALERT_WEBHOOK` 未設 | **要操作人親手俾一個 webhook URL**，我唔可以作域名 |

## 6. Beta 上線驗收標準（建議，操作人可改）

1. 快照層（255 上線卡）：價格／市值／1d delta 100%；7d/30d delta ≥95%；
   POP delta 覆蓋所有有 history 嘅卡；故事四語 100%；卡圖 100%；無 literal "top"。
2. 鏈條：連續 3 日 `verify_daily_run.py` 全 PASS（volume_floor 已帶 backfill 豁免）。
3. TAG date floor 已落地（07-29 前）。
4. 部署：staging 驗收 OK → 操作人拍板先推 production。

---
呢份文喺文檔樹嘅位置：想知**而家點** → `PROJECT_STATE.md`；想知**上階段學到咩** →
`STAGE_REVIEW_20260726.md`；想知**今階段做咩、點先算完** → 呢份。
